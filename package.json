{
  "name": "web",
  "scripts": {
    "predev": "npm run clean",
    "dev": "parcel watch --dist-dir ./public/assets/ assets/**/* --no-cache",
    "prebuild": "npm run clean",
    "build": "parcel build --no-source-maps --dist-dir ./public/assets/ assets/**/*",
    "clean": "del /s /q public\\assets"
  },
  "devDependencies": {
    "daisyui": "^3.2.1",
    "parcel": "2.9.3",
    "postcss": "8.4.26",
    "prettier": "^2.8.8",
    "prettier-plugin-go-template": "^0.0.13",
    "tailwindcss": "3.3.3"
  }
}
